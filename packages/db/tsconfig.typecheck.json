{
  // Same reason as every other package: the build must not emit tests into
  // dist, but a type error inside a test is a broken proof and must still fail
  // `pnpm typecheck`.
  "extends": "./tsconfig.json",
  "compilerOptions": { "noEmit": true },
  "include": ["src/**/*.ts"],
  "exclude": []
}
