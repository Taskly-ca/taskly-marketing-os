node_modules/
dist/
.turbo/
coverage/
*.tsbuildinfo
.DS_Store
*.log

# Secrets. This repo is PRIVATE, but never commit real values regardless.
.env
.env.*
!.env.example

# Collector target lists are DATA, not code — and may name real people/companies.
data/targets/*.local.json

# Generated by `pnpm --filter @tmos/worker report` — regenerate, never commit.
apps/worker/briefing.html

# Built by `node scripts/fetch-brain.mjs` on deploy: the FACT-SHEET and brain
# snapshot built from the marketplace repo (private). Never a source.
/brain/
